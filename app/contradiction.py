"""Contradiction worker — detects conflicts between stored memories."""

from typing import Any


async def check(memory: dict[str, Any]) -> list[dict[str, Any]]:
    # TODO: compare new memory against existing records and surface conflicts
    return []
